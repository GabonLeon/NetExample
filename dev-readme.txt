Hapus migrate
    dotnet ef migrations remove --project NetExample

Create migrate
    dotnet ef migrations add InitialCreate --project NetExample

Create first user (seeder)
    dotnet ef database update --project NetExample

username: 'string'
password: 'string'
(biar gampang)

vscode extension
    REST Client
    untuk test, ada di folder Request

Folder .vscode senggaja di include